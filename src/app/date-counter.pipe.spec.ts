import { DateCounterPipe } from './date-counter.pipe';

describe('DateCounterPipe', () => {
  it('create an instance', () => {
    const pipe = new DateCounterPipe();
    expect(pipe).toBeTruthy();
  });
});
